# 交接文档：上轮验收完成 / 站点账号功能整体移除（2026-06-11 凌晨）

> 写给下一个会话：本文档是完整断点，取代 `handoff-2026-06-10.md`（其待办已全部完成）。
> 历史背景见同目录 `local-gateway-roadmap.md`（Phase 1-7）与 `handoff-2026-06-10.md`（key 级独立通道 / 智能默认故障转移的实现细节）。

## 一、本轮做了什么（按时间序）

1. **上轮四项点击验收全部通过 ✅**（截图 /tmp/ccswitch-verify/40-53）：
   - 卡片「加入/移出」队列按钮往返：Cch 加入 → P1 徽章 + Anyrouter 顺延 P2 → 移出恢复（优先级=队列位置，往返无残留）
   - 智能默认绿框：把 Anyrouter 移出队列 → 路由页空队列渲染绿色「智能默认已生效」框；用下拉+加号把 Anyrouter 加回（toast「已添加到故障转移队列」，队列显示「1 Anyrouter」）→ **用户队列状态已还原**
   - 蓝色「路由服务未运行：配置可随时修改，启动服务后生效」提示；服务停止时开关/下拉/输入均可交互（输入框可获焦点环、下拉可展开）
   - 新 info 文案（key 级独立通道说明）+ 配置值 10/90/180/600 正确加载
2. 用户发来 metapi 导出（`../metapi-accounts-2026-06-10.json`，267KB，59 站点 67 账号）想测签到 → **随即改主意：「去掉签到功能」** → 完成签到移除后再次追加：**「站点账号功能没啥用了，删了」** → 整个 Gateway Accounts 功能移除。
3. 顺手修了一个**与本轮无关的既有 flaky 测试**（见 三.C）。
4. 最终用户确认「没问题了」。

## 二、站点账号功能移除（本轮主体改动）

**两步完成：先删签到（中间态已编译验收过），用户追加后整个功能删除。最终状态 = 整个功能不存在。**

### A. 已删除的文件

- `src/components/settings/GatewayAccountsPanel.tsx`
- `src/lib/api/gatewayAccounts.ts`
- `tests/components/GatewayAccountsPanel.test.tsx`
- `src-tauri/src/gateway_accounts.rs`（类型）
- `src-tauri/src/commands/gateway_accounts.rs`
- `src-tauri/src/services/gateway_accounts.rs`（含自动签到 worker、NewAPI 登录/签到/令牌列表 HTTP 客户端）
- `src-tauri/src/database/dao/gateway_accounts.rs`

### B. 已清理的引用点

- `src/components/settings/AuthCenterPanel.tsx`：删 import + `<GatewayAccountsPanel />` 挂载（认证页现在只有 OAuth 认证中心头 + GitHub Copilot + ChatGPT Codex OAuth 三段）
- `src/lib/api/index.ts`：删 `gatewayAccountsApi` 与 Gateway* 类型导出
- `src-tauri/src/lib.rs`：删 `mod gateway_accounts`、pub use 类型、`GatewayAccountService` 导出、4 个 Tauri 命令注册（list/add/delete_gateway_account、list_gateway_api_tokens）、`start_auto_checkin_worker` 启动调用
- `src-tauri/src/commands/mod.rs`、`services/mod.rs`、`database/dao/mod.rs`：删模块声明/导出
- `src-tauri/tests/provider_keys.rs`：删 MockGatewaySiteState、mock server、5 个 gateway 测试（含 2 个自动签到测试），清掉 axum/tokio::sync::Mutex 导入与 cc_switch_lib Gateway* 导入

### C. 数据库策略（重要，动过 schema.rs）

- **旧库的 `gateway_sites` / `gateway_site_accounts` / `gateway_checkin_logs` 表保留不动**（孤表，含用户 1 条 AnyRouter 账号数据，代码零引用，无风险可逆）
- 全新安装不再创建这三张表（`create_tables_on_conn` 里的调用已删）
- `migrate_v11_to_v12` / `migrate_v12_to_v13` 改为 **no-op 版本步进**（带注释说明），SCHEMA_VERSION 不变；`create_gateway_accounts_tables` / `ensure_gateway_account_schedule_columns` 函数已删
- i18n 无需改动（该面板全部用 defaultValue 内联中文，四语言文件里本就没有 gatewayAccounts key）

### D. 顺手修的既有 flaky 测试

`src-tauri/src/database/dao/usage_rollup.rs::test_rollup_merges_with_existing`：fixture 用 UTC 格式化日期，rollup SQL 用 `date(...,'localtime')` 分组 → 本地 0-8 点（UTC+8）运行必挂（本轮凌晨跑测试时撞上）。已改为 `Local.timestamp_opt(...)` 与实现同时区。**该文件不在本轮功能改动范围，是独立修复。**

## 三、测试与验收状态（全绿）

- `cargo test`（src-tauri）：lib **1583 passed**（含 rollup 修复），集成测试全过（provider_keys 15 passed，gateway 测试已删）
- `npm run typecheck` ✅；`npm run test:unit` **303 passed**（删掉 4 个面板测试后）
- release 已重建（`src-tauri/target/release/bundle/macos/CC Switch.app`，6-11 ~03:00）并点击验收：认证页无站点账号卡（截图 63/64），表单/签到 tab 中间态截图 60/61
- **用户已确认「没问题了」**

## 四、构建/验收工具链（沿用，全部踩坑已解决）

```bash
# 构建（pnpm 不在 PATH，shim 在 /tmp/ccswitch-verify/bin/pnpm = exec npx -y pnpm@10.24.0）
PATH=/tmp/ccswitch-verify/bin:$PATH npx tauri build   # 后台跑约 3 分钟
osascript -e 'tell application "CC Switch" to quit'; open ".../CC Switch.app"  # 进程名 cc-switch
CC_PROC="cc-switch" /tmp/ccswitch-verify/shot.sh <名>   # 窗口 900x600，PNG 2x（1800 宽时坐标÷2）
/tmp/ccswitch-verify/click.sh <x> <y>   # cliclick 真移动光标
```

- 顶部黄色环境变量横幅点 (800,33) 关；设置齿轮 (144,60)；设置 tab 行 y=113：通用(97)/路由(238)/认证(381)/高级(521)/使用统计(661)/关于(803)
- 翻页：先点内容区空白处再 `osascript key code 121/116`（**焦点在 tab 行时翻页键会切 tab**，踩过）
- 路由页：故障转移折叠区 (450,290) 展开；队列下拉 (425,406)、加号 (824,406)
- 测试 i18n：t() 返回 key 名；ConfirmDialog 确认按钮 name="common.confirm"

## 五、当前用户状态（勿弄乱）

- 主列表 5 张卡：Veai / Local 5100 Claude / Cch / **Anyrouter（使用中，队列 P1，Key 20/20）** / ccload
- 故障转移队列：仅 Anyrouter（已还原）；自动故障转移开关 = 关
- `../metapi-accounts-2026-06-10.json`（仓库外）是用户导出的真实凭据（anyrouter 8 账号 + 君の公益 muyuan.do 3 账号等），原本要测签到，**需求已取消**，文件留在原地未动

## 六、遗留小事（非阻塞）

- 路由队列「选择供应商添加到队列」下拉会列出 Claude Official（官方卡不支持路由却可手动入队）——既有行为，本轮观察到，未改
- 后端 get/update_circuit_breaker_config、get_circuit_breaker_stats 命令仍是无前端消费的死代码（上轮遗留）
- AutoFailoverConfigPanel / FailoverQueueManager 仍接受 disabled prop（无人传，兼容保留）
- 旧库三张 gateway_* 孤表将来如想彻底清掉，需要新增 DROP TABLE 迁移（属破坏性操作，须用户点头）

## 七、git

大量未提交改动（含上轮 key 级通道/智能默认 + 本轮功能移除）。**用户未要求 commit，不要主动 commit。**
